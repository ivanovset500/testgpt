#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ОбработчикиСобытий
Процедура ОбработкаПроведения(Отказ, РежимПроведения)
	//ПроведениеСервер.ПодготовитьНаборыЗаписейКПроведению(ЭтотОбъект);
	//
	//	
	//ПараметрыПроведения = Документы.ЛБ_ПрейскурантЦен.ПодготовитьПараметрыПроведения(Ссылка, Отказ);
	//Если Отказ Тогда
	//	Возврат;
	//КонецЕсли;
	//
	//// ФОРМИРОВАНИЕ ДВИЖЕНИЙ
	//
	//Документы.ЛБ_ОперацияПоЗалогу.СформироватьДвиженияУстановкаЦен(ПараметрыПроведения.УстановкаЦен,
	//	ПараметрыПроведения.ТаблицаРеквизитов, Движения, Отказ);


КонецПроцедуры

#КонецОбласти

#КонецЕсли
