
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Если НЕ ЗначениеЗаполнено(Запись.УстанавливатьОтклонение) Тогда
		Запись.УстанавливатьОтклонение = Перечисления.ЛБ_ОтклонениеОтБазовойЦены.Относительное;
	КонецЕсли;	
КонецПроцедуры
